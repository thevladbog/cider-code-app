# Continue installation even if optional dependencies fail
# This prevents build failures when native modules like 'usb' can't be compiled
ignore-optional=false
optional=true

# Suppress peer dependency warnings for React 19 compatibility
legacy-peer-deps=true

# Include optional dependencies by default (including Rollup binaries)
include=optional

# Reduce npm warnings about optional dependencies
audit-level=moderate
